# 🎯 最简单！使用在线工具生成签名密钥库

## 方法：使用在线工具（无需安装任何软件）

### 第一步：访问在线密钥库生成工具

打开以下任一网站：

1. **推荐工具 1**：https://keystore-file.com/
2. **推荐工具 2**：https://www.keystore-generator.com/
3. **备用工具**：https://android-keystore-generator.online/

### 第二步：填写密钥库信息

在在线工具中填写以下信息：

| 字段 | 填写内容 | 说明 |
|------|----------|------|
| **Keystore Name** | `release.keystore` | 密钥库文件名 |
| **Alias** | `release` | 密钥别名 |
| **Password** | `GaoQianLeMe2024!` | 密钥库密码（记住这个密码！） |
| **Key Password** | `GaoQianLeMe2024!` | 密钥密码（与密钥库密码相同） |
| **First Name** | `搞钱了么` | 姓名 |
| **Last Name** | `App` | 姓氏 |
| **Organization** | `GaoQianLeMe` | 组织名称 |
| **Organization Unit** | `Development` | 部门 |
| **City** | `Beijing` | 城市 |
| **State** | `Beijing` | 省份 |
| **Country** | `CN` | 国家代码（中国） |
| **Validity** | `25` | 有效期（年） |

### 第三步：下载密钥库文件

1. 点击 **Generate** 或 **Create** 按钮
2. 下载生成的 `release.keystore` 文件
3. **重要**：将此文件保存在安全的地方，不要删除！

### 第四步：转换为 Base64

访问以下在线工具：
- https://base64.guru/converter/encode/file
- https://www.base64encode.org/

操作步骤：
1. 点击 **Choose File** 或 **Browse** 按钮
2. 选择刚才下载的 `release.keystore` 文件
3. 点击 **Encode** 或 **Convert** 按钮
4. **复制** 生成的 Base64 字符串（可能很长，确保复制完整）

### 第五步：配置 GitHub Secrets

1. 访问：https://github.com/niklaus3016/gaoqianleme/settings/secrets/actions

2. 添加 4 个 Secrets：

   **Secret 1 - KEYSTORE_BASE64**：
   - 点击 "New repository secret"
   - Name: `KEYSTORE_BASE64`
   - Secret: 粘贴刚才复制的 Base64 字符串
   - 点击 "Add secret"

   **Secret 2 - KEYSTORE_PASSWORD**：
   - 点击 "New repository secret"
   - Name: `KEYSTORE_PASSWORD`
   - Secret: `GaoQianLeMe2024!`
   - 点击 "Add secret"

   **Secret 3 - KEY_PASSWORD**：
   - 点击 "New repository secret"
   - Name: `KEY_PASSWORD`
   - Secret: `GaoQianLeMe2024!`
   - 点击 "Add secret"

   **Secret 4 - KEY_ALIAS**：
   - 点击 "New repository secret"
   - Name: `KEY_ALIAS`
   - Secret: `release`
   - 点击 "Add secret"

### 第六步：触发构建

1. 访问：https://github.com/niklaus3016/gaoqianleme/actions
2. 点击 "Build Android APK" 工作流
3. 点击 "Run workflow" 按钮
4. 点击绿色的 "Run workflow" 按钮
5. 等待构建完成（约 5-10 分钟）

### 第七步：下载已签名的 APK

1. 构建完成后，点击对应的工作流运行记录
2. 滚动到页面底部
3. 在 "Artifacts" 部分找到 `release-apk`
4. 点击下载按钮
5. 解压 ZIP 文件，获得 `app-release.apk`

✅ **完成！** 这个 APK 已经签名，可以直接上传到小米应用商店。

---

## 📝 重要提醒：

### 密码管理：
- **记住密码**：`GaoQianLeMe2024!`
- **不要忘记**：如果忘记了无法找回
- **安全保存**：不要分享给任何人

### 文件管理：
- **保存好** `release.keystore` 文件
- **不要删除**：以后更新应用还需要用到
- **不要分享**：这是你的私有密钥

### 验证成功：
- GitHub Actions 构建摘要显示 "Signed and ready for app store submission"
- 下载的 APK 文件名为 `app-release.apk`

---

## 🔧 备用方案：下载 Java JDK

如果在线工具不好用，可以下载 Java JDK：

### Windows 用户：
1. 访问：https://www.oracle.com/java/technologies/downloads/
2. 下载 **Java SE Development Kit (JDK) 21**
3. 安装 JDK
4. 打开命令提示符（CMD）
5. 执行：`java -version` 验证安装

### Mac 用户：
1. 安装 Homebrew（如果没有）：`/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`
2. 安装 JDK：`brew install openjdk@21`
3. 验证安装：`java -version`

### Linux 用户：
1. Ubuntu/Debian：`sudo apt install openjdk-21-jdk`
2. 验证安装：`java -version`

安装 Java 后，我可以帮你生成密钥库！

---

## ❓ 遇到问题？

### 问题 1：在线工具打不开
- 尝试其他推荐工具
- 检查网络连接
- 使用 VPN（如果需要）

### 问题 2：Base64 转换失败
- 确保上传的是 `release.keystore` 文件
- 尝试使用不同的在线工具
- 检查文件是否损坏

### 问题 3：GitHub Actions 构建失败
- 检查 4 个 Secrets 是否都正确配置
- 确认 `KEYSTORE_BASE64` 是完整的 Base64 字符串
- 查看构建日志中的错误信息

### 问题 4：忘记密码
- 无法找回，必须重新生成密钥库
- 新密钥库会导致无法更新现有应用

---

## 🎉 推荐流程：

**最简单**：使用在线工具（无需安装任何软件）
1. 访问在线工具 → 填写信息 → 下载密钥库
2. 转换为 Base64 → 配置 GitHub Secrets
3. 触发构建 → 下载已签名的 APK

**如果在线工具不好用**：下载 Java JDK
1. 下载并安装 JDK
2. 告诉我，我帮你生成密钥库
3. 转换为 Base64 → 配置 GitHub Secrets
4. 触发构建 → 下载已签名的 APK

---

## 📞 需要帮助？

如果遇到任何问题，请告诉我：
1. 你使用的是哪个在线工具
2. 卡在哪一步
3. 看到的错误信息

我会帮你解决！
